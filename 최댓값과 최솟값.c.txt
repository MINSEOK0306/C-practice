#include<stdio.h>

int main() {
	int a, i, n;
	int max = 1000000;
	int min = -1000000;
	scanf("%d", &a); {
		for (i = 0; i < n; i++);
		scanf("%d", &a);

		if (a >= max) max = a;
		if (a <= min)min = a;
	} 
	printf("%d %d", min, max);
	return 0;
}